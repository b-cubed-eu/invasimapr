# Compact print method for invasimapr_fit

Internal print method. Not part of the public API.

## Usage

``` r
# S3 method for class 'invasimapr_fit'
print(x, ...)
```

## Arguments

- x:

  An object of class `invasimapr_fit`.

- ...:

  Unused; included for compatibility with the `print` generic.

## Value

Invisibly returns `x`, the `invasimapr_fit` object. Called for its side
effect of printing a compact summary to the console.
