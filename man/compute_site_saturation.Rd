% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_site_saturation.R
\name{compute_site_saturation}
\alias{compute_site_saturation}
\title{Compute site-only saturation S_s and global z-score S_s_z}
\usage{
compute_site_saturation(mode, comm_res, res_ids, mu_js = NULL)
}
\arguments{
\item{mode}{One of "opportunity_penalty", "modelled_dominance", "evenness_deficit".}

\item{comm_res}{Site × resident matrix (or data.frame/tibble) of counts for the
observed-based modes (will be coerced to matrix if needed).}

\item{res_ids}{Character vector of resident IDs (columns of \code{comm_res}).}

\item{mu_js}{Optional site × resident matrix (or data.frame/tibble) of expected
abundances for "modelled_dominance" (will be coerced to matrix if needed).}
}
\value{
List with \code{S_s}, \code{S_mu}, \code{S_sd}, \code{S_s_z}, and \code{S_js_z}.
}
\description{
Implements three site-only saturation options: "opportunity_penalty",
"modelled_dominance", and "evenness_deficit". Coerces non-matrix inputs with
\code{as.matrix()}. Returns S_s, global z, and a broadcast S_js_z for convenience.
}
\examples{
\dontrun{
sat = compute_site_saturation("evenness_deficit", comm_res, colnames(comm_res))
S_js_z = sat$S_js_z
}
}
